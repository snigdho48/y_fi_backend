name: Deploy to Server

on:
  push:
    branches:
      - master  # Trigger the action when changes are pushed to the main branch.

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    # Check out the repository
    - name: Checkout repository
      uses: actions/checkout@v2

    # Set up SSH key for deployment
    - name: Set up SSH key from .pem file
      run: |
        mkdir -p ~/.ssh
        echo "$PEM_FILE" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ec2-3-108-205-134.ap-south-1.compute.amazonaws.com >> ~/.ssh/known_hosts

    # Deploy to server
    - name: Deploy to server
      run: |
        # Connect to the server, remove old project, and deploy new one
        ssh -i ./App.pem -o StrictHostKeyChecking=no ubuntu@ec2-3-108-205-134.ap-south-1.compute.amazonaws.com << 'EOF'
          # Navigate to the project directory
          cd /home/ubuntu/projects/
          
          # Remove the old project (Be cautious, ensure you're deleting the right project directory)
          rm -rf y_fi_backend

          # Clone the latest version of the project from GitHub
          git clone https://github.com/snigdho48/y_fi_backend.git y_fi_backend

          # Navigate into the project directory
          cd y_fi_backend

          # Install dependencies (if you're using a virtual environment)
          python3 -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

          # Restart Gunicorn and Nginx (or your web server)
          sudo systemctl restart gunicorn
          sudo systemctl restart nginx
        EOF

    # Optional: Upload build logs (you can customize this part)
    - name: Upload build logs
      uses: actions/upload-artifact@v2
      with:
        name: build-logs
        path: build.log
